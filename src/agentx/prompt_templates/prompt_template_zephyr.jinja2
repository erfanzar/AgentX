{% for message in messages %}{% if message["role"] == "user" %}{{ "<|user|>\n" + message["content"] + eos_token + "\n" }}{% elif message["role"] == "system" %}{{ "<|system|>\n" + message["content"] + eos_token + "\n" }}<br>{% elif message["role"] == "assistant" %}{{ "<|assistant|>\n"  + message["content"] + eos_token + "\n" }}{% endif %}{% if loop.last and add_generation_prompt %}{{ "<|assistant|>\n" }}{% endif %}{% endfor %}